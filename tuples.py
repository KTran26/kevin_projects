coordinates = (4, 5)
#coordinates[1] = 10                                         # Examples of Tuples (4, 5), how it is immutable when you use them. Which means whatever is in tuples cannpt be used           # Which means it cannot be changed  
print(coordinates[1])                                       # Lists can be Changed Tuples cannot